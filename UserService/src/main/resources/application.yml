server:
  port: 8081
  error:
    include-message: always

spring:
  application:
    name: UserService

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

logging:
  level:
    org.springframework.security: DEBUG
